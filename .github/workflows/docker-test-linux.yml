name: Linux Tests (Docker)

on:
  workflow_dispatch:    # Manual trigger
    branches:
      - 'master'

#  push:
#    paths-ignore:
#      - '**.md'
#      - '**.yml'
#
#  pull_request:
#    paths-ignore:
#      - '**.md'
#      - '**.yml'

jobs:
  vm:
    runs-on: ubuntu-latest
    container:
      image: luismiaresse/pricelist-fetcher:latest
      options: --user root
    steps:
      - name: Install python dependencies and test
        run: |
          cd /home/luismi/pricelist-fetcher
          pip3 install -r requirements.txt pytest
          python3 -m pytest
